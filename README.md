# My Project
A brief description of your project.

## Setup
1. Install dependencies: `pip install -r requirements.txt` or `npm install`
2. Set environment variables (see .env.example)
3. Run: `python3 main.py` or `npm start`

## Deployment
- Port: 8080
- Build pack: Nixpacks
